#include<stdio.h>

int main()
{
	int a=10,b=5,c=23;

	if(a<b)
        {
	printf("the lesser number is %d\n",a);
	}
	if(b<c)
	{
	printf("the lesser number is %d\n",b);
	}
	if(c<a)
	{
        printf("the lesser number is %d\n",c);
	}
	else
	{
		printf("the leser number is %d\n",a);
	}
	 return 0;
}
